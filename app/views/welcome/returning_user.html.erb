<h1 class="center">Have you used this Health Pod before?</h1>

<table class="two-col options" data-option-group="user_data_returning_user" data-callback="validateForm" style="margin-top: 10rem;">
  <tr>
    <td style="text-align: right;">
      <div class="option btn-option large" data-value="1">Yes</div>
    </td>
    <td>
      <div class="option btn-option large" data-value="0">No</div>
    </td>
  </tr>
</table>

<%= form_tag returning_user_path, { :id => "form", :autocomplete => "off" } do |f| %>
  <%= hidden_field_tag 'user_data[returning_user]', '' %>
<% end %>

<script>
function validateForm(val) {
  $('#form').submit();
  // $('#next').removeClass('disabled');
}
</script>

<% content_for :footer_left do %>
  <label class="nav exit" for="exit-modal">Exit</label>
  <%= render :partial => 'partials/exit_modal', locals: { exit_url: root_url } %>
<% end %>
<% content_for :footer_center do %>
  <%= progress_bar %>
<% end %>
<% content_for :footer_right do %>
  <span style="visibility: hidden;"><%= next_button "$('#form').submit();" %></span>
<% end %>
